const express = require('express');
const app = express();

const bodyParser = require('body-parser');

const {generator} = require("./generator");

app.use('/generate',final)

app.listen(3120,()=>{
    console.log(`Server is running on port 3120`);
    
})